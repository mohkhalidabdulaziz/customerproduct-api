server:
  servlet:
    context-path: /customerproduct-api
spring:
  application:
    name: customerproduct-api
  datasource:
    url: jdbc:mysql://localhost:3306/dev-customerproduct
    username: root
    password: look123xyz
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  file:
    path: /Users/mac/Desktop/2024/etiqa/logs
  level:
    com.customerproduct.customerproductapi: DEBUG


